Basic NLP models.

Model definitions in model.py.
    - SimpleLSTM: Basic LSTM implementing a nn.LSTM. Accepts input_size, hidden_size, out_size as parameters
    - SimpleRNN: Basic RNN implementing a nn.RNN
    